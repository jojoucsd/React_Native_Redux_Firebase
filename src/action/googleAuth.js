import firebase from 'firebase';
// const provider = new firebase.auth.GoogleAuthProvider();

import {
    USER_NO_CHANGED,
    USER_CHANGED,
    LOG_IN_ERROR,
    IS_LOG_IN_LOADING
} from './types'

export const googleAuthencity = () => dispatch => {
    console.log('wer are here ')

}